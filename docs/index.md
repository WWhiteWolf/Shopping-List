# Where the Shopping List documents live

One line per file, live or history, so a session can tell whether a
document exists and which file is the home.

**`docs` is the live desk.** History lives in `docs-ref` when that
folder exists. If the index has no file for a thing, ask Patrick.
Do not invent a home.

**This file is not a reading order.** The opening read is
`handoff.md` only, per rule 2 of this project's `CLAUDE.md`.

**Keep it true or delete it.** A stale index is worse than none.

## The desk — in `docs`

- **`handoff.md`** — the opening read. Where the work stands and
  what is open. 3-Shopping: own repository and the App Store;
  2-Shopping is committed; the icon is still a teal square.
- **`handoff-history.md`** — the archive of the handoff. Newest
  sittings at the top. 1-Shopping and 2-Shopping are in it.
- **`pending.txt`** — Patrick's list.
- **`index.md`** — this file.
